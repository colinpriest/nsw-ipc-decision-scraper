# Compatible-release pins (~=) for reproducible runs (ISSUE-023). These bound
# critical parsing/LLM/Excel paths to a known-good minor series; bump
# intentionally and re-run the test suite before accepting upgrades. For a
# fully reproducible environment, pin exact versions in requirements.lock.txt.
requests~=2.32
curl_cffi~=0.12
beautifulsoup4~=4.12
openai~=1.109
python-dotenv~=1.0
pydantic~=2.10
pandas~=3.0
openpyxl~=3.1
pypdf~=5.9
